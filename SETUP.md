# 🛠️ Инструкция по настройке и запуску

## 📋 Предварительные требования

### 1. Создание Telegram бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен (он понадобится в настройках)

### 2. Установка зависимостей

#### Вариант А: Docker (рекомендуется)
- [Docker](https://www.docker.com/get-started)
- [Docker Compose](https://docs.docker.com/compose/install/)

#### Вариант Б: Локальная установка
- Python 3.11+
- PostgreSQL 15+
- pip или poetry

## 🚀 Быстрый запуск с Docker

### 1. Клонирование и настройка
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd telegram_ebbinghaus_bot

# Создайте файл настроек
cp .env.example .env
```

### 2. Настройка .env файла
Отредактируйте файл `.env`:
```env
# ОБЯЗАТЕЛЬНО: Токен вашего бота
BOT_TOKEN=your_bot_token_from_botfather

# Остальные настройки можно оставить по умолчанию
DATABASE_URL=postgresql://user:pass@postgres:5432/ebbinghaus_db
DEFAULT_TIMEZONE=Asia/Yekaterinburg
NOTIFICATION_TIME=07:00
DEBUG=false
```

### 3. Запуск
```bash
# Запуск всех сервисов
docker-compose up -d

# Создание миграций БД
docker-compose exec bot alembic upgrade head

# Проверка статуса
docker-compose ps
```

### 4. Проверка работы
```bash
# Проверка здоровья приложения
curl http://localhost:8000/health

# Просмотр логов
docker-compose logs bot
```

## 💻 Локальная разработка

### 1. Установка PostgreSQL
```bash
# Ubuntu/Debian
sudo apt-get install postgresql postgresql-contrib

# macOS
brew install postgresql

# Windows: скачайте с официального сайта PostgreSQL
```

### 2. Создание базы данных
```bash
# Подключитесь к PostgreSQL
sudo -u postgres psql

# Создайте базу данных
CREATE DATABASE ebbinghaus_db;
CREATE USER ebbinghaus_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE ebbinghaus_db TO ebbinghaus_user;
\q
```

### 3. Установка Python зависимостей
```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4. Настройка переменных окружения
```bash
# Создайте .env файл
cp .env.example .env

# Отредактируйте для локальной разработки
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://ebbinghaus_user:your_password@localhost:5432/ebbinghaus_db
DEBUG=true
```

### 5. Создание миграций
```bash
# Создайте начальную миграцию
python create_migration.py

# Или вручную:
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
```

### 6. Запуск бота
```bash
# Запуск в режиме polling (для разработки)
python run_local.py

# Или запуск FastAPI сервера
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 🌐 Настройка webhook (продакшн)

### 1. Получение SSL сертификата
```bash
# С Let's Encrypt (certbot)
sudo certbot --nginx -d yourdomain.com
```

### 2. Настройка nginx
```bash
# Отредактируйте nginx.conf для вашего домена
# Раскомментируйте HTTPS секцию
```

### 3. Установка webhook
```bash
# Замените YOUR_BOT_TOKEN и yourdomain.com
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://yourdomain.com/webhook"}'
```

### 4. Запуск продакшн версии
```bash
# С nginx reverse proxy
docker-compose --profile production up -d
```

## 🔧 Полезные команды

### Docker команды
```bash
# Просмотр логов
docker-compose logs bot -f

# Перезапуск сервиса
docker-compose restart bot

# Подключение к контейнеру
docker-compose exec bot bash

# Очистка и пересборка
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

### Миграции БД
```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1

# История миграций
alembic history
```

### Тестирование
```bash
# Проверка здоровья
curl http://localhost:8000/health

# Тестовое уведомление (замените USER_ID)
curl -X POST http://localhost:8000/api/test/notification/123456789

# Статистика пользователя
curl http://localhost:8000/api/users/123456789/stats
```

## 🐛 Устранение неполадок

### Проблема: Бот не отвечает
1. Проверьте правильность BOT_TOKEN
2. Убедитесь что webhook удален при локальном запуске
3. Проверьте логи: `docker-compose logs bot`

### Проблема: Ошибка подключения к БД
1. Убедитесь что PostgreSQL запущен
2. Проверьте DATABASE_URL в .env
3. Для Docker: `docker-compose ps postgres`

### Проблема: Уведомления не приходят
1. Проверьте статус планировщика: `curl http://localhost:8000/api/scheduler/status`
2. Убедитесь что часовой пояс правильный
3. Проверьте есть ли активные повторения у пользователя

### Проблема: Webhook не работает
1. Проверьте что домен доступен по HTTPS
2. Убедитесь что порт 443 открыт
3. Проверьте nginx конфигурацию

## 📊 Мониторинг

### Healthcheck эндпоинты
- `GET /health` - Общее здоровье приложения
- `GET /api/scheduler/status` - Статус планировщика

### Логи
- Docker: `docker-compose logs bot`
- Локально: проверьте файл логов указанный в LOG_FILE

### Метрики (можно расширить)
- Количество активных пользователей
- Статистика успешности повторений
- Производительность планировщика

## 🔒 Безопасность

### Переменные окружения
- Никогда не коммитьте .env файл
- Используйте сильные пароли для БД
- Ограничьте доступ к admin API

### Продакшн настройки
- Настройте firewall
- Используйте HTTPS
- Ограничьте webhook только IP Telegram
- Регулярно обновляйте зависимости

## 🎯 Следующие шаги

После успешного запуска:
1. Протестируйте основной workflow с ботом
2. Настройте резервное копирование БД
3. Добавьте мониторинг и алерты
4. Настройте CI/CD для автоматического деплоя

Удачи с изучением по методу Эббингауза! 🧠📚
