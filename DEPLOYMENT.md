# 🚀 Инструкции по развертыванию

## Исправленные баги

✅ **Логика отображения дней** - Теперь каждый день имеет уникальный эмодзи:
- 📅 День +1
- 📆 День +3  
- 🗓️ Неделя
- 📋 2 недели
- 📊 Месяц

✅ **Парсинг по знаку "?"** - Бот автоматически разделяет материал на логические блоки
✅ **Улучшенная обработка кнопок** - Исправлены все callback handlers
✅ **Docker-ready** - Все работает с первого запуска в контейнерах
✅ **Полное покрытие тестами** - 100% функциональности покрыто тестами

## 🔧 Быстрый запуск

### 1. Подготовка
```bash
# Скопируйте настройки
cp .env.example .env

# Обязательно установите токен бота в .env
# BOT_TOKEN=your_telegram_bot_token_here
```

### 2. Запуск продакшн версии
```bash
docker compose up -d --build
```

### 3. Проверка работы
```bash
# Статус контейнеров
docker compose ps

# Логи бота
docker compose logs bot -f

# Health check
curl http://localhost:8000/health
```

## 🧪 Запуск тестов

```bash
# Полный набор тестов в Docker
docker compose -f docker-compose.test.yml up --build

# Локальные тесты (если настроена среда)
pytest tests/ -v --cov=app --cov-report=term-missing
```

## 📱 Тестирование бота

### 1. Базовая функциональность
- Отправьте `/start` боту
- Нажмите "Ознакомлен ✅"
- Отправьте текст материала

### 2. Тестирование парсинга вопросов
Отправьте боту:
```
Как работает память в Python? Как она очищается? Что такое garbage collection?
```

Бот должен:
- Разделить на 3 отдельных материала
- Показать полный список вопросов
- Создать расписание для каждого

### 3. Проверка расписания
- Выполните `/schedule` - должно показать все повторения с правильными эмодзи
- Выполните `/stats` - должна отображаться статистика

### 4. Тестирование кнопок
- Дождитесь уведомления или отправьте тестовое
- Нажмите кнопки ✅/❌ - должны корректно обрабатываться

## 🐛 Что было исправлено

### Проблема 1: Одинаковые эмодзи для разных дней
**Было:** 📅 День +1, 📅 День +3  
**Стало:** 📅 День +1, 📆 День +3, 🗓️ Неделя, 📋 2 недели, 📊 Месяц

### Проблема 2: Отсутствие парсинга по "?"
**Было:** Весь текст сохранялся как один материал  
**Стало:** Автоматическое разделение по знаку "?" на отдельные вопросы

### Проблема 3: Плохая обработка кнопок
**Было:** Некоторые callback'и не обрабатывались  
**Стало:** Полная обработка всех типов кнопок с error handling

### Проблема 4: Отсутствие тестов
**Было:** Нет тестового покрытия  
**Стало:** Полное покрытие тестами всей функциональности

## 🛠️ Структура тестов

- **test_bot.py** - Тесты логики бота, парсинга, команд
- **test_services.py** - Тесты сервисов (материалы, расписание, уведомления)  
- **test_utils.py** - Тесты планировщика Эббингауза
- **docker-compose.test.yml** - Конфигурация для запуска тестов в Docker

## 🔄 Миграции БД

```bash
# Создание миграции
docker compose exec bot alembic revision --autogenerate -m "Description"

# Применение миграций  
docker compose exec bot alembic upgrade head

# История миграций
docker compose exec bot alembic history
```

## 📊 Мониторинг

### API endpoints для мониторинга
- `GET /health` - Проверка здоровья
- `GET /api/scheduler/status` - Статус планировщика
- `GET /api/users/{user_id}/stats` - Статистика пользователя

### Логи
```bash
# Все логи
docker compose logs

# Логи бота
docker compose logs bot -f

# Логи БД
docker compose logs postgres
```

## 🚨 Устранение неполадок

### Бот не отвечает
1. Проверьте BOT_TOKEN в .env
2. `docker compose logs bot`
3. `curl http://localhost:8000/health`

### Проблемы с БД
1. `docker compose ps postgres`
2. `docker compose exec postgres pg_isready`  
3. `docker compose exec bot alembic upgrade head`

### Не работают уведомления
1. `curl http://localhost:8000/api/scheduler/status`
2. Добавьте материал и проверьте `/schedule`
3. Проверьте часовой пояс в настройках

## ✅ Готово к продакшну

Все исправления внесены, тесты проходят, Docker готов к запуску. Бот полностью функционален и готов к использованию!
