# 🧠 Ebbinghaus Learning Bot

<div align="center">

![Python](https://img.shields.io/badge/python-3.11+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-009688.svg)
![Telegram Bot API](https://img.shields.io/badge/Telegram%20Bot%20API-20.7-2CA5E0.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-336791.svg)
![Docker](https://img.shields.io/badge/Docker-ready-2496ED.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**Telegram-бот для запоминания информации по методу кривой забывания Эббингауза**

[Возможности](#-возможности) • [Установка](#-установка) • [Использование](#-использование) • [API](#-api) • [Разработка](#-разработка)

</div>

---

## 📖 О проекте

**Ebbinghaus Learning Bot** — это умный Telegram-бот, который помогает эффективно запоминать любую информацию, используя научно обоснованный метод интервальных повторений Германа Эббингауза.

### 🎯 Почему это работает?

Метод основан на **кривой забывания Эббингауза**, которая показывает, что мы забываем информацию по экспоненциальной кривой. Повторение материала в определенные интервалы времени переносит информацию в долговременную память:

```
Интервалы повторений:
├─ Сразу (момент изучения)
├─ Через 20 минут
├─ Вечером в 20:00
├─ Через 1 день
├─ Через 3 дня
├─ Через 7 дней
├─ Через 14 дней
├─ Через 30 дней
└─ Раз в месяц (долговременная память)
```

---

## ✨ Возможности

### 🚀 Основной функционал

- **📝 Умное добавление материалов** - отправьте вопросы боту через символ `?`, и он автоматически разделит их на отдельные карточки
- **⏰ Автоматические напоминания** - получайте уведомления в 07:00 каждый день о материалах для повторения
- **🔄 Адаптивное обучение** - если не повторили материал, бот откатывает стадию и переносит на завтра
- **📊 Статистика прогресса** - отслеживайте успешность повторений и прогресс обучения
- **🌍 Поддержка часовых поясов** - уведомления приходят в удобное для вас время
- **📱 Интуитивный интерфейс** - встроенные кнопки для отметки успешности повторений

### 🔧 Технические особенности

- **Без дублей** - система гарантирует отсутствие дублирующих повторений
- **Месячная фаза** - материалы, достигшие 8+ стадии, повторяются раз в 30 дней
- **REST API** - доступ к статистике и расписанию через HTTP API
- **Debug-режим** - специальный эндпоинт для отладки расписания материалов
- **Docker-ready** - простое развертывание через Docker Compose

---

## 🚀 Установка

### Предварительные требования

- Docker и Docker Compose
- Токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/DaniilZebzeev/ebbinghaus-learning-bot.git
cd ebbinghaus-learning-bot
```

2. **Создайте файл `.env`:**
```bash
cp .env.example .env
```

3. **Отредактируйте `.env` и добавьте токен бота:**
```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql://user:pass@postgres:5432/ebbinghaus_db
DEFAULT_TIMEZONE=Asia/Yekaterinburg
NOTIFICATION_TIME=07:00
```

4. **Запустите приложение:**
```bash
docker compose up -d --build
```

5. **Примените миграции базы данных:**
```bash
docker compose exec bot alembic upgrade head
```

6. **Готово!** Найдите своего бота в Telegram и отправьте `/start`

### Локальная разработка (без Docker)

Требования: Python 3.11+, PostgreSQL 15+

```bash
# Установите зависимости
pip install -r requirements.txt

# Настройте PostgreSQL и обновите DATABASE_URL в .env
# Примените миграции
alembic upgrade head

# Запустите бота
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 📚 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Справка по использованию
- `/stats` - Посмотреть статистику обучения
- `/schedule` - Мои повторения

### Добавление материалов

Просто отправьте вопросы боту, разделяя их символом `?`:

```
Что такое кривая Эббингауза?
Какие интервалы повторений оптимальны?
Как работает долговременная память?
```

Бот автоматически:
1. Разделит текст на 3 отдельных вопроса
2. Создаст расписание повторений для каждого
3. Напомнит через 20 минут для первого повторения
4. Отправит вечернее напоминание
5. Продолжит по расписанию Эббингауза

### Отметка повторений

При получении напоминания используйте кнопки:

- ✅ **Повторил успешно** - переход к следующей стадии
- ❌ **Не повторил** - откат на предыдущую стадию, перенос на завтра

---

## 🔌 API

Приложение предоставляет REST API для интеграции:

### Основные эндпоинты

**Получить статистику пользователя:**
```http
GET /api/users/{user_id}/stats
```

**Получить расписание:**
```http
GET /api/users/{user_id}/schedule?days_ahead=7
```

**Получить материалы:**
```http
GET /api/users/{user_id}/materials?limit=10&active_only=true
```

**Debug расписания материала:**
```http
GET /api/users/{user_id}/materials/{material_id}/debug-schedule
```

**Проверка здоровья:**
```http
GET /health
```

**Статус планировщика:**
```http
GET /api/scheduler/status
```

### Пример ответа debug-эндпоинта

```json
{
  "user_id": 123456789,
  "material_id": 1,
  "material_text": "Что такое кривая Эббингауза?",
  "created_at": "2025-01-01T10:00:00",
  "current_stage": 5,
  "next_due_at": "2025-01-15T07:00:00",
  "next_repetition_type": "day_7",
  "timezone": "Asia/Yekaterinburg",
  "history": [
    {
      "stage": 0,
      "repetition_type": "immediate",
      "planned_at": "2025-01-01T10:00:00",
      "done_at": "2025-01-01T10:05:00",
      "result": "success"
    }
  ]
}
```

---

## 🏗️ Архитектура

```
ebbinghaus-learning-bot/
├── app/
│   ├── bot.py                    # Telegram bot логика
│   ├── main.py                   # FastAPI приложение
│   ├── config.py                 # Конфигурация
│   ├── database/
│   │   ├── connection.py         # Подключение к БД
│   │   ├── models.py             # SQLAlchemy модели
│   │   └── migrations/           # Alembic миграции
│   ├── services/
│   │   ├── user_service.py       # Управление пользователями
│   │   ├── material_service.py   # Управление материалами
│   │   ├── schedule_service.py   # Расписание повторений
│   │   └── notification_service.py # Уведомления
│   ├── utils/
│   │   └── ebbinghaus.py         # Логика интервалов Эббингауза
│   └── scheduler.py              # APScheduler для уведомлений
├── tests/                        # Unit-тесты
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Docker образ
├── requirements.txt              # Python зависимости
└── alembic.ini                   # Alembic конфигурация
```

### Технологический стек

- **Backend:** FastAPI, Python 3.11+
- **Bot Framework:** python-telegram-bot
- **Database:** PostgreSQL 15 + SQLAlchemy (async)
- **Scheduler:** APScheduler
- **Migrations:** Alembic
- **Caching:** Redis (optional)
- **Deployment:** Docker, Docker Compose
- **Testing:** pytest, pytest-asyncio

---

## 🧪 Тестирование

Проект покрыт unit-тестами:

```bash
# Запустить все тесты
pytest tests/ -v

# Запустить с покрытием
pytest tests/ -v --cov=app --cov-report=term-missing

# Запустить в Docker
docker compose -f docker-compose.test.yml up --build
```

**Покрытие тестами:**
- ✅ Логика интервалов Эббингауза (18/18 тестов)
- ✅ Месячная фаза повторений
- ✅ Откаты при неудачных повторениях
- ✅ Защита от дублей материалов

---

## 🔧 Разработка

### Создание миграции БД

```bash
# В Docker
docker compose exec bot alembic revision --autogenerate -m "Description"

# Локально
alembic revision --autogenerate -m "Description"
```

### Применение миграций

```bash
# В Docker
docker compose exec bot alembic upgrade head

# Локально
alembic upgrade head
```

### Логи

```bash
# Просмотр логов бота
docker compose logs bot -f

# Просмотр логов PostgreSQL
docker compose logs postgres -f
```

### Подключение к базе данных

```bash
docker compose exec postgres psql -U user -d ebbinghaus_db
```

---

## 📊 Мониторинг

### Health Checks

Приложение предоставляет эндпоинты для мониторинга:

```bash
# Проверка здоровья приложения
curl http://localhost:8000/health

# Статус планировщика
curl http://localhost:8000/api/scheduler/status
```

Ответ:
```json
{
  "status": "healthy",
  "scheduler": {
    "running": true,
    "jobs": 2
  },
  "bot_token_configured": true,
  "database_configured": true
}
```

---

## 🐛 Отладка

### Debug-эндпоинт для материалов

Используйте специальный эндпоинт для отладки расписания:

```bash
curl http://localhost:8000/api/users/123456789/materials/1/debug-schedule
```

Это покажет:
- Текущую стадию материала
- Следующее запланированное повторение
- Полную историю повторений с результатами

---

## 🛠️ Устранение неполадок

### Проблема: Бот не отвечает
1. Проверьте токен бота в `.env`
2. Убедитесь что бот запущен: `docker compose ps`
3. Проверьте логи: `docker compose logs bot`

### Проблема: Не работает БД
1. Проверьте статус PostgreSQL: `docker compose ps postgres`
2. Проверьте подключение: `docker compose exec postgres pg_isready`
3. Примените миграции: `docker compose exec bot alembic upgrade head`

### Проблема: Не приходят уведомления
1. Проверьте статус планировщика: `curl http://localhost:8000/api/scheduler/status`
2. Проверьте часовой пояс в настройках
3. Добавьте материал и проверьте расписание командой `/schedule`

---

## 🤝 Вклад в проект

Приветствуются pull requests! Для значительных изменений сначала откройте issue для обсуждения.

### Процесс разработки:

1. Fork репозитория
2. Создайте feature-ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Стиль кода

- Следуйте PEP 8
- Пишите type hints
- Документируйте функции с помощью docstrings
- Покрывайте новый код тестами

---

## 📝 Changelog

### v1.1.0 (2025-12-05)
- ✨ Добавлена месячная фаза для долговременной памяти
- 🐛 Исправлены дубли вопросов в повторениях
- 🔧 Улучшена логика интервалов Эббингауза
- 📊 Добавлен debug-эндпоинт для отладки расписания
- 💬 Улучшен текст при неудачном повторении
- 🧪 Добавлено 18+ unit-тестов
- 🗃️ Добавлено поле current_stage для отслеживания стадий

### v1.0.0 (2024-11-20)
- 🎉 Первый релиз
- 📝 Базовый функционал повторений
- 📱 Telegram бот интерфейс
- 📊 REST API
- 🐳 Docker поддержка

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для деталей.

---

## 👨‍💻 Автор

**Daniil Zebzeev**

- GitHub: [@DaniilZebzeev](https://github.com/DaniilZebzeev)
- Telegram: [@your_telegram](https://t.me/your_telegram)

---

## 🙏 Благодарности

- [Hermann Ebbinghaus](https://en.wikipedia.org/wiki/Hermann_Ebbinghaus) - за исследование кривой забывания
- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) - отличная библиотека для Telegram ботов
- [FastAPI](https://fastapi.tiangolo.com/) - современный и быстрый веб-фреймворк

---

<div align="center">

**⭐ Если проект вам помог, поставьте звезду на GitHub!**

Made with ❤️ and 🧠 by [Daniil Zebzeev](https://github.com/DaniilZebzeev)

</div>
