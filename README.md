# Telegram-бот для обучения по методу Эббингауза

Telegram-бот для эффективного запоминания материала с использованием кривой забывания Эббингауза и системы интервальных повторений.

## 🔧 Основные возможности

- **Парсинг по знаку "?"** - автоматическое разделение материала на логические блоки
- **Интервальные повторения** по методу Эббингауза (20 мин → вечером → +1 день → +3 дня → +7 дней → +14 дней → +30 дней)
- **Ежедневные уведомления** в 07:00 по местному времени
- **Статистика обучения** с процентом успешности
- **Полное покрытие тестами** всего функционала
- **Docker-ready** с первого запуска

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Method
```

### 2. Настройка переменных окружения
```bash
cp .env.example .env
# Отредактируйте .env файл, обязательно установите BOT_TOKEN
```

### 3. Запуск в Docker
```bash
# Запуск продакшн версии
docker compose up -d --build

# Проверка логов
docker compose logs bot -f
```

### 4. Проверка работы
```bash
# Проверка здоровья приложения
curl http://localhost:8000/health

# Проверка API
curl http://localhost:8000/
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Запуск всех тестов в Docker
docker compose -f docker-compose.test.yml up --build

# Запуск тестов локально (если у вас настроена среда)
pytest tests/ -v --cov=app --cov-report=term-missing
```

### Структура тестов
- `tests/test_bot.py` - тесты для бота (парсинг, команды, callback'и)
- `tests/test_services.py` - тесты для сервисов (материалы, расписание, уведомления)
- `tests/test_utils.py` - тесты для утилит (планировщик Эббингауза)

## 📖 Использование бота

### Команды
- `/start` - Начать работу с ботом
- `/help` - Справка по использованию  
- `/schedule` - Мои повторения (расписание)
- `/stats` - Статистика обучения

### Добавление материала
1. Отправьте боту текст с вопросами/темами
2. Используйте знак "?" для разделения на логические блоки:
   ```
   Как работает память в Python? Как она очищается? Что такое garbage collection?
   ```
3. Бот автоматически создаст отдельные материалы для каждого вопроса
4. Получайте напоминания и отмечайте результат повторений

### Пример работы с материалом
**Ввод:**
```
Как работает память в Python? Как она очищается?
```

**Результат:** Бот создаст 2 отдельных материала:
1. "Как работает память в Python?"
2. "Как она очищается?"

Каждый получит собственное расписание повторений.

## 🏗️ Архитектура

### Структура проекта
```
app/
├── bot.py              # Основная логика бота
├── main.py             # FastAPI приложение
├── config.py           # Конфигурация
├── scheduler.py        # Планировщик уведомлений
├── database/
│   ├── models.py       # SQLAlchemy модели
│   ├── connection.py   # Подключение к БД
│   └── migrations/     # Alembic миграции
├── services/
│   ├── material_service.py      # Работа с материалами
│   ├── schedule_service.py      # Расписание повторений
│   ├── notification_service.py  # Уведомления
│   └── user_service.py          # Пользователи
└── utils/
    ├── ebbinghaus.py   # Логика интервалов Эббингауза
    └── timezone_utils.py # Работа с часовыми поясами
```

### Используемые технологии
- **Python 3.11** - основной язык
- **FastAPI** - веб-фреймворк для API
- **python-telegram-bot** - Telegram Bot API
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL** - база данных
- **APScheduler** - планировщик задач
- **Docker & Docker Compose** - контейнеризация
- **pytest** - тестирование

## 🔧 Разработка

### Локальная разработка
```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# Установка зависимостей
pip install -r requirements.txt

# Настройка БД
alembic upgrade head

# Запуск бота
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Создание миграций
```bash
# Создание новой миграции
docker compose exec bot alembic revision --autogenerate -m "Description"

# Применение миграций
docker compose exec bot alembic upgrade head
```

### Отладка
```bash
# Просмотр логов
docker compose logs bot -f

# Подключение к контейнеру
docker compose exec bot bash

# Подключение к БД
docker compose exec postgres psql -U user -d ebbinghaus_db
```

## 📊 API Endpoints

- `GET /` - Информация о приложении
- `GET /health` - Проверка здоровья
- `POST /webhook` - Webhook для Telegram
- `GET /api/users/{user_id}/stats` - Статистика пользователя
- `GET /api/users/{user_id}/materials` - Материалы пользователя
- `GET /api/users/{user_id}/schedule` - Расписание пользователя
- `GET /api/scheduler/status` - Статус планировщика
- `POST /api/test/notification/{user_id}` - Тестовое уведомление

## 🔐 Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | ✅ |
| `DATABASE_URL` | URL подключения к PostgreSQL | ✅ |
| `WEBHOOK_URL` | URL для webhook (продакшн) | ❌ |
| `DEFAULT_TIMEZONE` | Часовой пояс по умолчанию | ❌ |
| `NOTIFICATION_TIME` | Время уведомлений (07:00) | ❌ |
| `DEBUG` | Режим отладки | ❌ |

## 📈 Мониторинг

### Health Check
```bash
curl http://localhost:8000/health
```

### Проверка планировщика
```bash
curl http://localhost:8000/api/scheduler/status
```

### Логи
```bash
# Логи бота
docker compose logs bot

# Логи БД
docker compose logs postgres

# Все логи
docker compose logs
```

## 🛠️ Устранение неполадок

### Проблема: Бот не отвечает
1. Проверьте токен бота в `.env`
2. Убедитесь что бот запущен: `docker compose ps`
3. Проверьте логи: `docker compose logs bot`

### Проблема: Не работает БД
1. Проверьте статус PostgreSQL: `docker compose ps postgres`
2. Проверьте подключение: `docker compose exec postgres pg_isready`
3. Примените миграции: `docker compose exec bot alembic upgrade head`

### Проблема: Не приходят уведомления
1. Проверьте статус планировщика: `curl http://localhost:8000/api/scheduler/status`
2. Проверьте часовой пояс в настройках
3. Добавьте материал и проверьте расписание командой `/schedule`

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Вклад в разработку

1. Создайте форк репозитория
2. Создайте ветку для новой функции
3. Внесите изменения с тестами
4. Убедитесь что все тесты проходят
5. Создайте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории.