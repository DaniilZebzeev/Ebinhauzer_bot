# 🚀 Инструкции по деплою исправленной версии

## Проблема была исправлена:
- Добавлены немедленные уведомления при создании материала
- Пользователь теперь получает напоминания через 25 минут и вечером в 20:00
- Улучшен UX - понятно когда повторять материал

## Деплой на Ubuntu сервер:

### 1. Загрузите обновленные файлы:
```bash
# Перейти в папку проекта
cd ~/ebbinghaus-bot

# Скопировать обновленный app/bot.py с Windows на сервер
# Через SFTP в Termius или любым другим способом
```

### 2. Перезапуск с исправлениями:
```bash
# Остановка контейнеров
docker compose down

# Пересборка с новым кодом
docker compose up -d --build

# Проверка логов
docker compose logs bot --tail 20
```

### 3. Тестирование:
1. Отправьте боту любой текст материала
2. Сразу получите инструкции по повторению
3. Через 25 минут должно прийти напоминание ⏰
4. Вечером в 20:00 еще одно напоминание 🌆
5. Завтра в 07:00 утреннее напоминание 📅

### 4. Проверка работы:
```bash
# Мониторинг уведомлений в логах
docker compose logs bot -f | grep -i "reminder\|notification\|sendMessage"

# Проверка статуса
curl http://localhost:8000/health
```

## Что исправлено:
✅ Немедленные инструкции при добавлении материала
✅ Автоматические напоминания через 25 минут  
✅ Вечерние напоминания в 20:00
✅ Утренние напоминания в 07:00
✅ Inline кнопки для отметки результата

Теперь система работает по методу Эббингауза как задумано!
