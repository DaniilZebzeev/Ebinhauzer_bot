# Миграция для добавления поля is_acknowledged

## На сервере выполните:

```bash
# 1. Остановка контейнеров
docker compose down

# 2. Загрузка обновленных файлов app/bot.py и app/database/models.py

# 3. Перезапуск с пересборкой
docker compose up -d --build

# 4. Создание миграции для нового поля
docker compose exec bot alembic revision --autogenerate -m "Add is_acknowledged field to users"

# 5. Применение миграции
docker compose exec bot alembic upgrade head

# 6. Проверка
docker compose logs bot --tail 10
```

## Альтернативный способ - ручное добавление поля:

```bash
# Если автомиграция не сработает, добавьте поле вручную:
docker compose exec postgres psql -U user -d ebbinghaus_db -c "
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_acknowledged BOOLEAN DEFAULT FALSE;
"
```

## Тестирование новых функций:

1. **Тест ознакомления:**
   - Отправьте `/start` новому пользователю
   - Нажмите "Ознакомлен ✅"
   - Снова отправьте `/start` - должно показать краткое приветствие

2. **Тест команды /schedule:**
   - Добавьте материал
   - Выполните `/schedule`
   - Должно показать расписание с эмодзи и группировкой по дням

Все готово! 🚀
